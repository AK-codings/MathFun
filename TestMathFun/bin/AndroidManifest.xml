<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="air.testmathfun"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="21" />

    <application
        android:name="com.activeandroid.app.Application"
        android:allowBackup="true"
        android:icon="@drawable/math"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="hr.foi.air.main.MainActivity"
            android:label="@string/app_name" >
	        <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
        </activity>
        
        <activity android:name="hr.foi.air.main.Nickname"></activity>
        <activity android:name="hr.foi.air.main.LevelSelect"></activity>
        <activity android:name="hr.foi.air.main.Question"></activity>
        <activity android:name="hr.foi.air.main.Result"/>  
        <activity android:name="hr.foi.air.main.Rules"/>
        <!-- Baza za aplikaciju -->
        <meta-data android:name="AA_DB_NAME" android:value="baza.db" />
        <meta-data android:name="AA_DB_VERSION" android:value="2" />
      	  
    </application>

</manifest>
